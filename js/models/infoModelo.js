export default class Informacao {
        constructor(id, capitulo, titulo, subtitulo, imagem, texto) {
                this.id = id
                this.capitulo = capitulo
                this.titulo = titulo
                this.subtitulo = subtitulo
                this.imagem = imagem
                this.texto = texto
        }
}