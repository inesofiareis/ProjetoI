export default class Gamificacao {
    constructor(nome, descricao, img, pontos) {
        this.nome = nome
        this.descricao = descricao
        this.img = img
        this.pontos = pontos
    }
}